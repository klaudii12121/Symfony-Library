{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title_category_list'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'title_category_list'|trans }}</h1>
    {% if pagination is defined and pagination.items|length %}
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>{{ knp_pagination_sortable(pagination, 'label_id'|trans, 'category.id') }}<i class="fa fa-fw fa-sort"></i></th>
                    <th>{{ knp_pagination_sortable(pagination, 'label_category_name'|trans, 'category.categoryName') }}<i class="fa fa-fw fa-sort"></i></th>
                    <th>{{ 'label_more'|trans }}</th>
                </tr>
            </thead>
            <tbody>
                {% for category in pagination.items %}
                    <tr>
                        <td>{{ category.id }}</td>
                        <td>{{ category.categoryName }}</td>
                        <td>
                            <a href="{{ url('category_show', {id: category.id}) }}" title="{{ 'action_view'|trans }}">
                                {{ 'action_view'|trans }}
                            </a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
        <div class="navigation text-center">
            {{ knp_pagination_render(pagination) }}
        </div>
    {% else %}
        <p>
            {{ 'message_empty_list'|trans }}
        </p>
    {% endif %}
{% endblock %}
